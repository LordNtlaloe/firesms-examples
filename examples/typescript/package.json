{
  "name": "firesms-typescript",
  "version": "1.0.0",
  "description": "Fire SMS example app using TypeScript",
  "scripts": {
    "start": "node --env-file=.env --loader ts-node/esm src/send.ts",
    "bulk": "node --env-file=.env --loader ts-node/esm src/bulk.ts",
    "build": "tsc",
    "start:compiled": "node --env-file=.env dist/send.js",
    "bulk:compiled": "node --env-file=.env dist/bulk.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
